# Local Server Port
server.port=8080
# Local Database Configuration (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/blinq?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Vansh@sql16
# JPA / Hibernate Configuration
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# JWT Configuration for Local Development
jwt.secret=ThisIsMySuperSecretKeyForLocalTesting123!
jwt.expiration=86400000
